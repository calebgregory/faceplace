h1 Everyone
.containter
  .row
    .person(ng-repeat="person in everyone.everyone | objToArr")
      a.btn.btn-primary(href="/#/everyone/{{person._id}}")
        img.everyone(ng-src="{{person.photo}}")
